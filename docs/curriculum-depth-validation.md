# 第一輪內容補強：本機驗收紀錄

日期：2026-09-08。範圍：24 課、72 個能力目標、144 道新題；完整題庫共 1,490 題。尚未部署。

## 自動驗證

- `npm test`：正式組建、全部題庫驗證及 95 項測試通過。
- 149,000 次答案格式檢查；新題的數值答案另以獨立公式核對，推理題做內容交叉審核。
- 多組種子驗證新題可達、6 題練習包含 3 個補強能力、55 個 Boss 區域含圖表或推理題，以及新舊題組的版本保留。
- 以實際遷移後的記憶體 SQLite 執行儲存邏輯：13 項案例涵蓋進度恢復、完整題目判定、歷史分子分母配對、跨頁作答、記憶檢查及複習重複推進保護。
- `npm run lint`、`npx tsc --noEmit --incremental false`、報告一致性檢查及 `git diff --check` 通過。

## 實際瀏覽器操作

使用本機開發環境的 Demo 與合成進度，未登入真實帳號或寫入正式資料。

- G7：6 題練習、座標題答錯後提示及更正、記憶檢查排程、數字題 Enter 送出均正常。
- 新題只有答對後顯示「Why this works」；展開後 Enter 不會誤跳下一題。
- 390 × 844 手機尺寸下，G7 比例表與 G12 成對差異分析長表皆無水平溢出；實測頁寬 390、表格寬 324 像素。
- Boss 實際抽出多選題及新比例表題，答對後可顯示解析。
- Daily Review 實際出現 G12 原題及同能力的 NEW CHALLENGE；切題時解析重新隱藏。

## 驗證邊界與後續

正式登入後的瀏覽器恢復流程未以真實帳號端到端操作；保存與恢復由實際 SQL 測試及畫面邏輯審核支撐。G10 獨立表格與完整 Boss 結算未另做瀏覽器驗收。合成進度、網路攔截及手機尺寸設定已還原。本輪未新增資料庫遷移，也未部署。

開發模式曾出現 title／description metadata 的 hydration 警告；當時觀察的差異位於框架 StreamingMetadata，未見題組內容差異。完整警告未保留，根因尚未完全確認，不能視為已修正。

Daily Review 保留既有的動態到期清單；另一頁改變清單，或完成回應遺失後重送，可能需要重新載入。持久化 Review 題組與網路重送恢復仍屬後續工作。

229 課仍待逐能力審核；24 課的本輪目標及延伸缺口詳見[覆蓋報告](curriculum-depth-report.md)。本輪不宣稱完整 AP／IB 覆蓋。
